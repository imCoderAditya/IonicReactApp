import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.ionicreactapp',
  appName: 'ionicreactapp',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
